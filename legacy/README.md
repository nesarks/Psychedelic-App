# Legacy

Pre-restructure prototypes. **Do not add features here** — use as UX and algorithm reference when building `frontend/` and `backend/`.

| Directory | Description |
|-----------|-------------|
| [prototype-ui/](prototype-ui/) | Static HTML/CSS/JS (“Mindful Journey”) — prep, trip, journal, microdose |
| [flask-tolerance-calculator/](flask-tolerance-calculator/) | Flask `POST /api/calculate-tolerance` — port to FastAPI module M5 in v1 |

To run prototype UI locally: open `prototype-ui/index.html` in a browser or serve the folder with any static server.
